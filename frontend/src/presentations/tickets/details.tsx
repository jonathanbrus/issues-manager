"use client";

import { Box, Typography } from "@mui/material";

export const TicketDetailsPage: React.FC<{}> = (props) => {
  return (
    <Box>
      <Typography>Ticket Details</Typography>
    </Box>
  );
};
